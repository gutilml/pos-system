# Plan — Feature 057

1. Add `highlightIndex` + `onKeyDown` to `ProductsWorkspace` lookup (mirror `SearchBar`).
2. In `ProductEditorForm` load effect: ignore `AbortError`; remove `t` from deps; clear error on success.
3. Vitest + docs/pending update.
