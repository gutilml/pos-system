# Feature 098 — Frontend parent package editor guards

## Global rules

Ensure you adhere to our global rules in `PROJECT_CONTEXT.md` and `.cursorrules`.

## Status

**Done** — Parent under barcodes; pc parent disables weight; category copied editable. Partial supersede of **076** (pc + weight no longer allowed).

## Summary

Product editor UX for parent packages: field order, disable sell-by-weight when parent unit is `pc`, copy category from parent (still editable), keep derived cost read-only and shared inventory.

## Depends on

BE **097**.
