# Tasks: Feature 008 - Frontend Shift UI & State Hydration

- [x] 1. Update `useCartStore.ts` with Zustand's `persist` middleware.
- [x] 2. Create `useShiftStore.ts` to manage shift state and backend API integration.
- [x] 3. Build the `OpenShiftModal` and `CloseShiftModal` UI components with Tailwind CSS.
- [x] 4. Build the `ShiftGate` wrapper component and integrate it into the root/Register layout.
- [x] 5. Implement the `CashierMenu` with the "Close Shift" trigger.
- [x] 6. Write frontend tests for cart persistence and ShiftGate rendering logic.
