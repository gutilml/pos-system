# Feature 078 — Frontend shift history / reconciliation

## Global rules

Ensure you adhere to our global rules in `PROJECT_CONTEXT.md` and `.cursorrules`.

## Status

**Done** — CashierMenu → Shift history modal: list (`listShifts`) + detail (`getShiftDetail`) with events/totals; EN/ES; tests.

## Summary

UI to browse store shifts (closed and open) and view reconciliation detail from Feature **077** APIs. Complements live shift gate (**008** / **018**).

## Depends on

BE **077**.

## Out of scope

* Opener/closer name labels (**080**); drawer policy UX beyond existing **031**.
