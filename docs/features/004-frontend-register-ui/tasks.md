# Tasks: Feature 004 - Frontend Core Register UI

- [x] 1. Install Zustand and set up the `useCartStore` with cart math logic.
- [x] 2. Create the `SearchBar`, `CartItemRow`, and `CheckoutFooter` React components using Tailwind CSS.
- [x] 3. Implement the auto-select (highlight text on focus) behavior in the `CheckoutFooter` payment input.
- [x] 4. Assemble `RegisterScreen` as a full-height, single-column layout.
- [x] 5. Write Vitest / React Testing Library tests for the Zustand store math and the footer component interactions.
