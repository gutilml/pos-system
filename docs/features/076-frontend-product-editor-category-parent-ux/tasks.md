# Task Checklist

## Global rules

Ensure you adhere to our global rules in `PROJECT_CONTEXT.md` and `.cursorrules`.

## Frontend Tasks

- [x] 1. Add `childCostFromParentPreview` (+ unit conversion) in `productPricing.ts` with Vitest.
- [x] 2. Searchable category combobox + trailing **→ Add category ←**.
- [x] 3. Inline category create panel → `createCategory` → select + margin apply.
- [x] 4. Searchable parent picker; derive cost; force inventory off; prefill weight UOM.
- [x] 5. Weight UOM chips when `sellByWeight`; `buildBody` sends chip value.
- [x] 6. EN/ES strings in `messages.ts`.

## Test Tasks

- [x] 7. Extend `ProductEditorForm.test.tsx` for A–C.
- [x] 8. Unit tests for `childCostFromParentPreview`.

## Docs Tasks

- [x] 9. Triad Done; pending frontend Done; catalog **076** Done; commit `feat(076): …`.
