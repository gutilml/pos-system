# Feature: Frontend scale early pair and auto-read

## Acceptance Criteria

1. [x] Banner when Web Serial supported and no granted port (dismissible for session).
2. [x] Settings always shows Connect / Reconnect scale.
3. [x] Weight modal auto-reads with `allowPrompt: false` when ports exist.
4. [x] Manual Read uses `allowPrompt: true`; numpad always works.
5. [x] Vitest + catalog Done.
