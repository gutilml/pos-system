# Feature 099 — Frontend scale early pair and auto-read

## Global rules

Ensure you adhere to our global rules in `PROJECT_CONTEXT.md` and `.cursorrules`.

## Status

**Done** — Vitest green. FE-only; no backend.

## Summary

Pair the USB/serial scale early after register open (Connect banner / Settings). Weight modal auto-reads via remembered Web Serial ports. Numpad and Read from scale remain as fallback.

## Out of scope

Backend; live weight streaming; auto-confirm cart lines.
