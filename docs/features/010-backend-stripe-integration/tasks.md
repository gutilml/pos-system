# Tasks: Feature 010 - Backend Stripe Checkout Integration

- [x] 1. Add `stripe-java` dependency and configure API keys in environment properties.
- [x] 2. Create `StripePaymentService` with MXN-configured Checkout Session generation.
- [x] 3. Implement the `BigDecimal` to cents conversion logic safely within the service.
- [x] 4. Build the Webhook endpoint in `PaymentController` with strict signature verification.
- [x] 5. Write unit tests for the currency conversion and mock the checkout session creation.
- [x] 6. Review `docs/pending_features` and update it by crossing out or removing any fulfilled requirements related to Stripe or payments.
