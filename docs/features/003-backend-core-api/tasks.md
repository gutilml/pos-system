# Tasks: Feature 003 - Backend Core API & Services

- [x] 1. Create DTO records/classes for Products, Categories, and Transactions.
- [x] 2. Implement `ProductService` with margin-calculation business logic.
- [x] 3. Implement `TransactionService` emphasizing strict `BigDecimal` recalculations.
- [x] 4. Implement `ProductController` and `TransactionController` with `/api/v1/` routing.
- [x] 5. Write Unit tests for Service math and WebMvcTests for API endpoints.
