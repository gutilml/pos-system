# Tasks — 088 Backend store default tax rate

## Global rules

Ensure you adhere to our global rules in `PROJECT_CONTEXT.md` and `.cursorrules`.

- [x] 1. Allowlist + validate `default_tax_rate` in preferences.
- [x] 2. Settings GET/PATCH (+ optional `/me`).
- [x] 3. Tests; pending + catalog Done; commit `feat(088): …`.
