# Tasks: Feature 016 - Frontend Discount UI & Visual Math

- [x] 1. Update `useCartStore` to hold discount state and perfectly replicate the backend cascading math logic.
- [x] 2. Update `CartItemRow` to include an item-level discount input, strikethrough pricing, and an exclusion badge.
- [x] 3. Update `CheckoutFooter` to include a global discount input and display total savings.
- [x] 4. Format the API payload to include the discount percentages when executing checkout.
- [x] 5. Write frontend tests explicitly verifying the math for item-level, global-level, and excluded combinations.
- [x] 6. Review `docs/pending feature/frontend.md` and update it by crossing out or removing any fulfilled requirements related to discounts.
