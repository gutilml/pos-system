# Feature 070 — Frontend credit movements description

## Status

**Done** — Ledger shows stored `description`; section titled Movements / Movimientos.

## Summary

Display Feature **069** `description` instead of raw `PAYMENT`/`CASH` enums. Section chrome uses Movements (EN) / Movimientos (ES); row text does not re-translate when locale changes.
