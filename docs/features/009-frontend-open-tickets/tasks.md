# Tasks: Feature 009 - Frontend Open Tickets (Tabs) Management

- [x] 1. Refactor `useCartStore.ts` to support multiple tickets and an `activeTicketId`.
- [x] 2. Update all existing cart actions (`addItem`, `removeItem`, computations) to scope strictly to the active ticket.
- [x] 3. Build the `TicketTabs.tsx` UI component using Tailwind CSS.
- [x] 4. Integrate `TicketTabs` into `RegisterScreen.tsx`.
- [x] 5. Fix and update frontend tests to pass with the new state structure.
- [x] 6. Review `docs/pending_features` and update it by crossing out or removing any fulfilled requirements related to this feature.
