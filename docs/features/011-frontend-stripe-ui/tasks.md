# Tasks: Feature 011 - Frontend Stripe UI & Payment Polling

- [x] 1. Install `qrcode.react` (or similar) dependency in the frontend.
- [x] 2. Implement API functions for creating a session and checking transaction status.
- [x] 3. Build the `StripePaymentModal` UI displaying the generated QR code.
- [x] 4. Implement the 3-second interval polling logic to watch for backend status changes.
- [x] 5. Wire the success state to automatically clear the active ticket and close the modal.
- [x] 6. Write frontend tests to verify the polling and state transition logic.
- [x] 7. Review `docs/pending_features/frontend.md` and update it by crossing out or removing any fulfilled requirements related to this feature.
