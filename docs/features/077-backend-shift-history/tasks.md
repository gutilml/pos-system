# Tasks — 077 Backend shift history

## Global rules

Ensure you adhere to our global rules in `PROJECT_CONTEXT.md` and `.cursorrules`.

- [x] 1. Repository queries for store shift list (+ optional status).
- [x] 2. `GET /api/v1/shifts` list + `GET /api/v1/shifts/{id}` detail with events/totals.
- [x] 3. Tests; pending + catalog Done; commit `feat(077): …`.
