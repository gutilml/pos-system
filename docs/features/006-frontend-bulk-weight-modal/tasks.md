# Tasks: Feature 006 - Frontend Bulk & Weight Input Modal

- [x] 1. Update `useCartStore.ts` with the interception logic and `pendingWeightProduct` state.
- [x] 2. Create the Web Serial API helper utility (`serialScaleHelper.ts`) for hardware scale integration.
- [x] 3. Build the `WeightModal.tsx` component with a responsive touchscreen numpad.
- [x] 4. Mount the modal conditionally within `RegisterScreen.tsx`.
- [x] 5. Write frontend tests to verify the modal rendering and state interception logic.
