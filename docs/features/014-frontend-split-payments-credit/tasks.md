# Tasks: Feature 014 - Frontend Split Payments & Customer Credit UI

- [x] 1. Update the Zustand store to hold an array of payment tenders and an assigned `customerId` per ticket.
- [x] 2. Build the `CustomerSearch` component to fetch and assign users.
- [x] 3. Build the `CheckoutModal` to handle adding multiple payment methods and calculating the `balanceDue`.
- [x] 4. Implement the interception logic: require a customer assignment if `CREDIT` is selected.
- [x] 5. Format the API payload to match the backend split-payment DTO and execute the checkout request.
- [x] 6. Write frontend tests verifying the tender math and button validation states.
- [x] 7. Review `docs/pending_features/frontend.md` and update it by crossing out or removing any fulfilled requirements related to this feature.
