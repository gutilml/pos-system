# Tasks — 079 Backend shift audit stamps

## Global rules

Ensure you adhere to our global rules in `PROJECT_CONTEXT.md` and `.cursorrules`.

- [x] 1. Migration: shift opener/closer (+ optional transaction `created_by`).
- [x] 2. Stamp open/close (and sale create) from JWT; DTO fields.
- [x] 3. Tests; pending + catalog Done; commit `feat(079): …`.
