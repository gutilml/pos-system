# Feature 073 — Frontend closed tickets + reimburse

## Global rules

Ensure you adhere to our global rules in `PROJECT_CONTEXT.md` and `.cursorrules`.

## Status

**Done** — Footer `Clear | Discount | Assign | Previous tickets | Pay`; closed tickets modal list/detail/reimburse against **072**. CARD blocked in UI.

## Summary

Register footer: `Clear | Discount | Assign customer | Previous tickets | Pay` (smaller Clear/Discount/Pay). Previous tickets opens list → review → full/partial reimburse (line + qty). CARD tickets not reimbursable (message). EN/ES.

## Out of scope

* CARD refund UI beyond blocked state; cashier-own filter; product name on line items (BE has productId only).
