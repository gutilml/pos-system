# Task Checklist — Feature 060

## Backend Tasks

- [x] 1. Empty-q list + ungate create/search; get/update/delete APIs.
- [x] 2. Hard delete when balance is zero (clear ledger + transaction FK).
- [x] 3. `enableCustomerCredit` on `UserResponseDTO` / AuthService.
- [x] 4. Docs triad + pending + catalog.

## Test Tasks

- [x] 5. JUnit: list/create/update/delete/credit-gate/auth flag.
