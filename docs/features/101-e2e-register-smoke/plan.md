# Plan — 101 E2E register smoke

## Global rules

Ensure you adhere to our global rules in `PROJECT_CONTEXT.md` and `.cursorrules`.

## Approach

1. Extend `RegisterShiftReimburseRegressionTest` with a sell-by-weight product line and username JSON assertions.
2. Extend `registerFlow.regression.test.ts` with cart weight confirm.
3. Triad + catalog; delete pending E2E bullet.
